# Master Upstart and SysVinit configuration file

# Customize location of Master binary (especially for development testing).
#MASTER="/usr/local/bin/master"

# Use MASTER_OPTS to modify the daemon startup options.
#MASTER_OPTS="--showlog 4 --printlog 5"

# If you need Master to use a specific environment variable, it can also be specified here.
export SOME_ENVIRONMENT_VARIABLE=somevalue

