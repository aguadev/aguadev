dojo.provide("t.plugins.view.addview.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl("t.plugins.view.addview.test", 
                         dojo.moduleUrl("t", "plugins/view/addview/test.html"));
}
