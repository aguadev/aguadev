dojo.provide("t.plugins.view.getset.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl("t.plugins.view.getset.test", 
                         dojo.moduleUrl("t", "plugins/view/getset/test.html"));
}
