dojo.provide("t.plugins.view.removeview.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl("t.plugins.view.removeview.test", 
                         dojo.moduleUrl("t", "plugins/view/removeview/test.html"));
}
