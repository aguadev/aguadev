dojo.provide("t.plugins.form.validationtextbox.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.form.validationtextbox.test", 
         dojo.moduleUrl("t", "plugins/form/validationtextbox/test.html"),
         20000
     );
}
