dojo.provide("t.unit.plugins.request.downloads.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.unit.plugins.request.downloads.test", 
          dojo.moduleUrl("t", "unit/plugins/request/downloads/test.html"));
}
