dojo.provide("t.unit.plugins.request.simplesearch.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.unit.plugins.request.simplesearch.test", 
          dojo.moduleUrl("t", "unit/plugins/request/simplesearch/test.html"));
}
