dojo.provide("t.plugins.graph.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.graph.test", 
          dojo.moduleUrl("t", "plugins/graph/test.html"));
}
