dojo.provide("t.plugins.workflow.grid.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.workflow.grid.test", 
          dojo.moduleUrl("t", "plugins/workflow/grid/test.html"));
}
