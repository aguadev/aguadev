dojo.provide("t.plugins.workflow.runworkflow.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.workflow.runworkflow.test", 
          dojo.moduleUrl("t", "plugins/workflow/runworkflow/test.html"));
}
