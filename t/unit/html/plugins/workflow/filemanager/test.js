dojo.provide("t.plugins.workflow.filemanager.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.workflow.filemanager.test", 
          dojo.moduleUrl("t", "plugins/workflow/filemanager/test.html"));
}
