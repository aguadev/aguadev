dojo.provide("t.plugins.admin.paramfunction.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.admin.paramfunction.test", 
          dojo.moduleUrl("t", "plugins/admin/paramfunction/test.html"));
}
