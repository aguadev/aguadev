dojo.provide("t.unit.plugins.apps.version.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.unit.plugins.apps.version.test", 
          dojo.moduleUrl("t", "unit/plugins/apps/version/test.html"));
}
