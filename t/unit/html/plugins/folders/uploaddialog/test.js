dojo.provide("t.plugins.workflow.io.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl("t.plugins.project.uploaddialog.test", 
                         dojo.moduleUrl("t", "plugins/project/uploaddialog/test.html"));
}
