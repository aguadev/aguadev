dojo.provide("t.plugins.folders.filedrag.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.plugins.folders.filedrag.test", 
          dojo.moduleUrl("t", "plugins/folders/filedrag/test.html"));
}
