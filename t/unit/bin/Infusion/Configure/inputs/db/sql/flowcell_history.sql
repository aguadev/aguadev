CREATE TABLE  flowcell_history (
  flowcell_history_id int(10) unsigned NOT NULL AUTO_INCREMENT,
  flowcell_id int(10) unsigned NOT NULL,
  flowcell_barcode varchar(45) NOT NULL,
  update_timestamp timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  fpga_version varchar(100) NOT NULL,
  rta_version varchar(100) NOT NULL,
  run_length varchar(100) NOT NULL,
  indexed enum('no','yes') NOT NULL DEFAULT 'no',
  status_id int(10) unsigned NOT NULL,
  operator varchar(100) DEFAULT NULL,
  description varchar(250) DEFAULT NULL,
  recipe varchar(250) DEFAULT NULL,
  machine_name varchar(45) NOT NULL,
  location varchar(250) NOT NULL,
  run_start_date date DEFAULT NULL,
  priority_flowcell int(2) DEFAULT NULL,
  location_md5sum varchar(45) DEFAULT NULL,
  comments text,
  fail_code_id int(10) DEFAULT NULL,
  user_code_and_ip text,
  PRIMARY KEY (flowcell_history_id),
  KEY fc_idx (flowcell_id)
)
